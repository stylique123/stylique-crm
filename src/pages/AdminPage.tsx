/**
 * STYLIQUE CRM — Admin shell.
 * Settings only. Team lives on /team. Exceptions removed.
 */
import SettingsPage from './SettingsPage';

export default function AdminPage() {
  return <SettingsPage />;
}